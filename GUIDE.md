# دليل رفيق (Rafiq)

> الصفحة الرئيسية للمشروع بالإنجليزي: [README.md](README.md). هون الدليل الكامل بالعربي — الأوامر، الصفحات، وين بتنحفظ بياناتك، وكيف تبني.

تطبيق دسكتوب: تضيف نموذج LLM (Anthropic / OpenAI / DeepSeek / Ollama / أي مزوّد متوافق مع OpenAI)، تعطيه مهمة، ورفيق يشتغل عليها فعلياً على جهازك (ملفات، أوامر shell، عمليات) — مع نظام صلاحيات يوقفه ويسألك قبل أي إجراء خطير.

## شو بيعمل

- **محادثة** مع أي نموذج، مع بث مباشر للرد، Markdown، وعرض تفكير الموديل.
- **مجلد لكل محادثة**: لما تحدده، الموديل بيقدر يقرأ ويعدّل الملفات ويشغّل أوامر جوّاه — والكتابة والأوامر بتاخد إذنك من جوّا المحادثة.
- **مرفقات** (صور، PDF، ملفات نصية) بالمحادثة وبالمهام — بالزر، بالسحب والإفلات، أو بلصق صورة.
- **خطة → مهام**: ابعت خطة بالمحادثة والموديل بيقسمها لمهام، بتشوف حالة كل وحدة جوّا المحادثة، وبيستنى لحتى يخلصوا ويرد عليك بالنتايج.
- **مهام بالتوازي**: لحد 100 مهمة بنفس الوقت (بتغيّر الحد من الإعدادات)؛ المهام اللي بتعدّل نفس الملفات بتاخد دورها، ومهمة بتقدر تستنى غيرها تخلص قبلها. وإشعار بيطلعلك وين ما كنت إذا مهمة بدها إذنك أو خلصت.
- **الرد ما بيوقف لما تطلع**: إذا رحت لصفحة تانية والموديل عم يرد، الرد بيكمل، ولما ترجع بتلاقيه وين وصل.
- **نسخة git لكل مهمة**: لما المجلد مشروع git، كل مهمة بتشتغل بنسخة معزولة (worktree) من المجلد متل ما هو (مع الملفات اللي ما عملتلها commit)، فمهام نفس المشروع بيشتغلوا مع بعض، وبعدين تغييراتها بتنطبق على مجلدك بدون أي commit على فرعك.
- **مراجعة التغييرات**: بصفحة كل مهمة على مجلد git في «التغييرات»: الملفات والـ diff، وزر يرجّع كل اللي عملته المهمة، أو يطبّق التغييرات اللي ما انطبقت لحالها.
- **تعليمات المشروع**: حط ملف `RAFIQ.md` (أو `AGENTS.md`) بالمشروع، وأي محادثة أو مهمة عليه بتقراه أول: أوامر البناء، الأسلوب، شو ممنوع يلمس.
- **خوادم MCP**: من الإعدادات بتربط أي خادم MCP (أمر محلي أو HTTP)، وأدواته بتصير عند الوكيل بصلاحية خاصة فيها، وقيمه السرّية بـ Windows Credential Manager.
- **الويب والمتصفح**: الوكيل بيقرأ صفحات وملفات PDF، وبيبحث عبر Brave أو Tavily أو SearXNG إذا ضبطتهم، وعنده متصفح حقيقي (Microsoft Edge بملف تعريف خاص) بيضغط وبيكتب وبيجرّب مواقع localhost.
- **التحكم بسطح المكتب**: مطفي افتراضياً — صورة شاشة، فأرة وكيبورد على أي تطبيق، مهمة وحدة بالمرة، وكل حركة بصلاحيتها.
- **مهام مجدولة وقوالب**: مهمة كل كم دقيقة، أو يومياً، أو بأيام محددة؛ وقوالب للمهام اللي بتتكرر (في أربعة جاهزين).
- **حدود المزوّدين**: عدد محدود من الطلبات لنفس المفتاح بنفس الوقت (الباقي بيستنى بدل ما يوقع بخطأ 429)، إعادة محاولة تلقائية لما المزوّد يضغط أو يوقع، ونموذج احتياطي لكل وكيل.
- **المصروف والحدود**: كل نداء بينحسب (توكنز وسعر) لكل وكيل ولكل يوم، وبتقدر تحط حد يومي أو شهري بالدولار — لما يوصله رفيق بيوقف يبعت للمزوّد.
- **تسجيل صوتي**: زر المايك بصندوق الكتابة بيحوّل حكيك لنص باستخدام مزوّد عندك أصلاً (OpenAI أو Groq أو Azure).
- **تعديل وتفريع**: عدّل سؤال بعتّه من قبل وابعته من جديد، أو فرّع المحادثة من أي نقطة لتجرّب اتجاه تاني بدون ما تخسر هاي.
- **تحديث من جوّا التطبيق**: من صفحة «من نحن» بيفحص إذا في نسخة أجدد، بينزّلها موقّعة، وبيعيد التشغيل عليها.
- **تقرير للمشاكل**: من الإعدادات ← الاستهلاك، بيحفظ ملف JSON فيه نسخة التطبيق وإعداداتك وأسماء المزوّدين — بلا مفاتيح ولا محتوى محادثاتك.
- **بالخلفية**: لما تسكّر النافذة رفيق بيضل بجنب الساعة، وبيطلعلك إشعار ويندوز لما مهمة تخلص أو بدها إذنك، وبتقدر تخليه يشتغل مع ويندوز.
- **ربط أنظمة التتبّع**: Jira و Linear و GitHub Issues و GitLab Issues — بتشوف مهامك المسندة إلك، وبتقدر تشاور عليها بالمحادثة وتكتب تعليق وتعلّمها مكتملة.
- **صفحة «شغلي»**: كل المهام المسندة إلك من كل الحسابات المربوطة بمكان واحد — بحث، فلترة بالمنصّة، تجميع حسب المشروع أو الحالة، ولوحة جانبية فيها كل تفاصيل المهمة وتعليقاتها مع أزرار: علّق، علّمها مكتملة، ناقشها بالمحادثة، أو نفّذها كمهمة.
- **أوامر بالمحادثة**: `/` للأوامر، `@` لملفات مجلد المحادثة (بأيقونات الملفات من ويندوز نفسه)، `#` لمهام حساباتك المربوطة.
- **تحكّم بالتوكنز**: `/إعدادات` بيضبط طول الرد ولغته والحرارة والتفكير (reasoning) وتشغيل الأدوات لهالمحادثة، و`/لخّص` بتطوي الرسائل القديمة بملخص فبتوقف تنبعت كلها مع كل رسالة.

### أوامر المحادثة (`/`)

| الأمر | شو بيعمل |
| --- | --- |
| `/مهمة` | تذكر مهمة من حساباتك المربوطة (نفس `#`) |
| `/ملف` | تذكر ملف من مجلد المحادثة (نفس `@`) |
| `/أرفق` | ترفع صورة أو ملف |
| `/مجلد` | تحدد مجلد العمل لهالمحادثة |
| `/نموذج` | تبدّل النموذج اللي بيرد |
| `/إعدادات` | طول الرد، لغته، الحرارة، التفكير، والأدوات |
| `/لخّص` | تطوي المحادثة بملخص عشان توفّر توكنز |
| `/أعد` | تحذف آخر رد وتجرّب من جديد |
| `/انسخ` | تنسخ آخر رد للحافظة |
| `/صدّر` | تحفظ المحادثة كملف Markdown |
| `/خلصت` | تعليق على مهمة التتبّع + تعليمها مكتملة |
| `/جديد` | محادثة جديدة |
| `/مساعدة` | كل الأوامر والاختصارات |

### صفحة التصاميم

صمّم الواجهة قبل ما تبرمجها:

1. **`/impeccable init`** — تسع أسئلة عن المنتج والمستخدم والإجراء الأساسي والنبرة والهوية والاتجاه والمراجع والقيود. اللي بتتركه فاضي بينفترض وبينقال لك شو انفترض.
2. **مجلد التصميم (اختياري)** — بتحدده قبل ما تبدأ: كل نسخة جديدة من التصميم بتنحفظ فيه كملف HTML، والنموذج بيقدر يقرأ ملفات المشروع من نفس المجلد.
3. **جلسة التصميم** — نفس الشات العادي (أوامر، مرفقات، صلاحيات، بث مباشر) جنب **معاينة حيّة**: كل ما النموذج يرجّع مستند HTML، بينعرض فوراً بعرض موبايل/تابلت/شاشة، وبتناقشه بالشات وبتشوف التعديل.
4. **بدء البرمجة** — بيبعت القرارات + كود الواجهة المعتمد لمحادثة جديدة أو محادثة شغّالة أو مهمة بالخلفية، مع تعليمات إنه يقرأ المهارات قبل ما يبدأ.

### المهارات (Skills)

مهارات التصميم مدمجة بالتطبيق — ما بدها تنزيل ولا إعداد، و**بتشتغل مع كل النماذج** لأن رفيق بيمرّرها كأدوات عادية (`skill_list` و `skill_read`)، مو كميزة خاصة بمزوّد.

- `impeccable` — طريقة الشغل: الـ brief، وظيفة الشاشة، النظام (خط/لون/مسافات/شكل/حركة)، الحالات، العربي وRTL، قائمة مكافحة الـ slop، وشكل المخرجات.
- مجموعة [Emil Kowalski](https://github.com/emilkowalski/skills) (رخصة MIT، مرفقة بملف الرخصة): `emil-design-eng`, `animate`, `animation-vocabulary`, `review-animations`, `improve-animations`, `find-animation-opportunities`, `apple-design`, `prototype`, `pick-ui-library`, `ask-sonner`, `animate-expo`, `write-swift`.

بتقدر تضيف مهاراتك من زر **«أضف مهارة»** بتبويب المهارات (بتختار مجلد فيه `SKILL.md`)، أو **«الصق مهارة»** لو عندك النص بس. بتتخزن بـ `%APPDATA%/Rafiq/skills` وبتطلع مع المدمجة، وبتقدر تحذفها أو تستبدل وحدة مدمجة بنفس الاسم.

قراءة المهارات ما بتظهر بالمحادثة — بتشوف رفيق «عم يفكّر» وبعدها الرد، بدون خطوات داخلية.

### قائمة المحادثات

بحث فوري بالعناوين، تجميع حسب الوقت (مثبّتة / اليوم / أمس / آخر ٧ أيام / أقدم)، تثبيت محادثة فوق، إعادة تسمية (دبل كليك على العنوان)، وحذف بتأكيد. كل صف بيعرض لوغو المزوّد، آخر نشاط، عدد الرسائل، مجلد المحادثة إذا محدد، وعلامة إذا كانت متلخّصة.

### الشكل

الشريط الجانبي وقائمة المحادثات بتتسحب من الحد بينهم (دبل كليك بيرجّع العرض الافتراضي)، والشريط بينطوي لأيقونات بس. ومن **الإعدادات → الشكل** بتظلبط عرض المحادثة (ضيّق/متوسط/عريض/كامل)، وبتخفي قائمة المحادثات، وبترجّع كل شي للافتراضي. كل هالخيارات بتنحفظ على جهازك.

## البنية

> تفاصيل أوسع — كل طبقة ومسؤوليتها، و«وين أعدّل لو بدي…» — بملف [ARCHITECTURE.md](ARCHITECTURE.md).


- `apps/desktop` — واجهة Tauri 2 + React + TypeScript + Tailwind (RTL/عربي بالكامل).
- `apps/agent` — الـ backend: FastAPI + SQLAlchemy (SQLite) + `litellm` (طبقة توحيد الموديلات) + نظام الأدوات (filesystem/shell/process) + `keyring` لتخزين مفاتيح الـ API بشكل آمن في نظام التشغيل.

## المتطلبات

- Node.js 20+ و pnpm (`corepack enable` أو `npm i -g pnpm`)
- Python 3.11+
- Rust (`rustup`) — لازم لبناء وتشغيل تطبيق Tauri الفعلي
- على ويندوز: Visual Studio Build Tools (مكوّن "Desktop development with C++") لازم يكون مثبّت لـ Rust/Tauri

## الإعداد لأول مرة

```bash
# الواجهة
pnpm install

# الـ backend
cd apps/agent
python -m venv .venv
./.venv/Scripts/pip install -e ".[dev,copilot]"
```

## التشغيل (تطبيق دسكتوب حقيقي)

```bash
cd apps/desktop
pnpm tauri dev
```

هاد بيشغّل Vite تلقائياً، وبعدها Rust بيولّد port وtoken عشوائيين، يشغّل الـ backend (`apps/agent/.venv/Scripts/python.exe -m uvicorn rafiq_agent.main:app`) كـ sidecar محلي، ويمرّر الإعدادات للواجهة عبر أمر Tauri (`get_api_config`). ما في حاجة تشغّل الـ backend يدوياً.

## التشغيل للتطوير فقط (متصفح، بدون Tauri)

لو ما بدك تشغّل نافذة Tauri (مثلاً لتجربة سريعة بالمتصفح)، شغّل الطرفين يدوياً:

```bash
# طرفية 1
cd apps/agent
RAFIQ_TOKEN=dev-token ./.venv/Scripts/python -m uvicorn rafiq_agent.main:app --port 8765

# طرفية 2
cd apps/desktop
pnpm dev
```

بهاد الوضع، `apps/desktop/.env` بيحدد `VITE_API_BASE`/`VITE_API_TOKEN` (لازم يطابقوا القيم يلي شغّلت فيها الـ backend).

## بناء نسخة تثبيت (بدون أوامر وقت التشغيل)

```bash
cd apps/desktop
pnpm build:app      # المثبّت بس
pnpm release        # فحص كامل + بناء + نسخة جاهزة للنشر بـ release/ مع SHA256SUMS.txt
```

`pnpm build:app` بيعمل تلات شغلات:

1. **يجمّد الـ backend** بـ PyInstaller (`apps/agent/rafiq-agent.spec`) لمجلد `apps/agent/dist/rafiq-agent/` فيه `rafiq-agent.exe` وكل اللي بيلزمه (litellm، keyring، المهارات المدمجة) — بدون Python على الجهاز.
2. **يبني تطبيق Tauri** ويحطّ مجلد الـ backend جوّاه كـ resource، وبيطلّع مثبّت NSIS بـ
   `apps/desktop/src-tauri/target/release/bundle/nsis/رفيق_<version>_x64-setup.exe`.
3. **يوقّع المثبّت للتحديث التلقائي** بالمفتاح الخاص اللي برّا المستودع (`~/.tauri/rafiq-updater.key`،
   بينعمل مرة وحدة بـ `pnpm tauri signer generate -w ~/.tauri/rafiq-updater.key`). بدونه البناء بيكمل
   بس بلا توقيع، و`pnpm release` ما بيطلّع `latest.json` — يعني النسخ المثبّتة ما بتشوف التحديث.

للنشر: ارفع للـ GitHub release (tag `v<version>`) التلات ملفات اللي بـ `release/`:
`Rafiq_<version>_x64-setup.exe` و `SHA256SUMS.txt` و `latest.json`.

بعد التثبيت بيصير في اختصار «رفيق» على سطح المكتب وبقائمة ابدأ. التطبيق بيولّد port وtoken
عشوائيين، بيشغّل `agent/rafiq-agent.exe` المرفق بدون نافذة طرفية، وبيسكّره لما تسكّر النافذة.
وقت التطوير (`pnpm tauri dev`) بيضل يستخدم الـ venv عشان تعديلات Python تشتغل مباشرة.

سجلّ الـ backend بالنسخة المثبّتة: `%APPDATA%/Rafiq/agent.log`.

## وين بتنحفظ بياناتك

| الشي | المكان |
| --- | --- |
| المحادثات، المهام، التصاميم، الإعدادات (قاعدة البيانات) | `%APPDATA%/Rafiq/rafiq.db` |
| المرفقات | `%APPDATA%/Rafiq/attachments` |
| مهاراتك الخاصة | `%APPDATA%/Rafiq/skills` |
| ملفات أي مهمة أو تصميم **ما حددت إله مجلد** | `Documents/Rafiq/tasks/<اسم-الجلسة>` و `Documents/Rafiq/designs/<اسم-التصميم>` |
| مفاتيح الـ API | خزنة ويندوز (Credential Manager) — مو بقاعدة البيانات |

مجلد الملفات بينعرض بصفحة **الإعدادات → مكان الملفات** مع زر يفتحه، وبتقدر تغيّره بمتغيّر البيئة
`RAFIQ_WORKSPACE_DIR`. كل هالمسارات خارج مجلد التثبيت، فتحديث التطبيق أو إعادة تثبيته ما بتلمس بياناتك.

## الفحص قبل البناء

```bash
cd apps/desktop && pnpm check    # TypeScript + ESLint + اختبارات الواجهة
cd apps/agent && .venv/Scripts/ruff check rafiq_agent tests && .venv/Scripts/python -m pytest -q
```

اختبارات الواجهة على المنطق الصافي (مُخفِّض بث الرد، الاتجاه والـ bidi، التخطيط، الوقت)،
واختبارات المحرّك على الخدمات + مرور HTTP حقيقي على التطبيق مع نموذج وهمي (بدون شبكة ولا مفاتيح).

## الأمان

- كل مفاتيح الـ API بتنخزن بخزنة نظام التشغيل (`keyring`) — قاعدة البيانات ما بتشوف غير مرجع مبهم.
- كل طلب على الـ backend لازم يحمل نفس الـ token يلي Rust ولّده وقت الإقلاع — عملية ثانية عالجهاز ما بتقدر "تتحدث" مع الوكيل.
- أي أداة تكتب على الملفات أو تنفّذ أوامر تحتاج إذنك الصريح افتراضياً (قابل للتعديل من الإعدادات لكل فئة أداة).
